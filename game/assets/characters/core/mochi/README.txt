Mochi — Cat Racers core assets

All PNGs use RGBA transparency. Animation frames have a common 640x640 canvas, 48 px bottom safe margin, shared baseline, and cast-normalized overall sprite size.
Use selection/select_sprite.png for the side-view character select art and selection/portrait.png for the head portrait.
Frame order is numeric; each animation sequence loops in ascending order.
