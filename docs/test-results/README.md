# Recorded test evidence

These logs and telemetry files record the local functional, collision, and Mac release checks described in `../../BUILD_STATUS.md` and `../COLLISION_REVIEW.md`.

Before publication, machine-specific absolute project paths in release and export logs were replaced with `<project-root>`. Relative output paths, test results, timing measurements, and engine messages were preserved.
